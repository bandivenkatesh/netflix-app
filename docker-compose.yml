services:
  backend:
    build:
      context: ./backend
    container_name: netflix-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    container_name: netflix-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
